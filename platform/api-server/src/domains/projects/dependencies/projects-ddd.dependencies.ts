/**
 * Projects DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Projects Domain Module - Composition root
 *
 * DDD: Composition root for projects domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  FrameRepositoryAdapter,
  ProjectRepositoryAdapter,
  RiskClassGateRepositoryAdapter,
  ScaleQuotumRepositoryAdapter,
} from "@aetherlab/adapters/projects";
import { getIdGeneratorService } from "@aetherlab/adapters";
import type { AdapterDynamoDBClient } from "@aetherlab/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateProject,
  ExecuteDecideProjectScaleQuota,
  ExecuteGetProject,
  ExecuteGetRiskClassGateMatrix,
  ExecuteListProjects,
  ExecuteSubmitProjectProblemFrame,
} from "@aetherlab/services/projects/usecases";
// Ports (interfaces)
import type {
  FrameRepository,
  ProjectRepository,
  RiskClassGateRepository,
  ScaleQuotumRepository,
} from "@aetherlab/services/projects/ports";

export interface ProjectsDomainModule {
  repos: {
    frames: FrameRepository;
    projects: ProjectRepository;
    riskClassGates: RiskClassGateRepository;
    scaleQuota: ScaleQuotumRepository;
  };
  useCases: {
    frames: {
      create: ExecuteSubmitProjectProblemFrame;
    };
    projects: {
      create: ExecuteCreateProject;
      get: ExecuteGetProject;
      list: ExecuteListProjects;
    };
    riskClassGates: {
      get: ExecuteGetRiskClassGateMatrix;
    };
    scaleQuota: {
      get: ExecuteDecideProjectScaleQuota;
    };
  };
}

export function buildProjectsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ProjectsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      frames?: FrameRepository;
      projects?: ProjectRepository;
      riskClassGates?: RiskClassGateRepository;
      scaleQuota?: ScaleQuotumRepository;
    } = {};

    obj.frames = new FrameRepositoryAdapter(dynamoClient);
    obj.projects = new ProjectRepositoryAdapter(dynamoClient);
    obj.riskClassGates = new RiskClassGateRepositoryAdapter(dynamoClient);
    obj.scaleQuota = new ScaleQuotumRepositoryAdapter(dynamoClient);
    return {
      frames: obj.frames!,
      projects: obj.projects!,
      riskClassGates: obj.riskClassGates!,
      scaleQuota: obj.scaleQuota!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    frames: {
      create: new ExecuteSubmitProjectProblemFrame(executionContext, idGenerator, repos.frames),
    },
    projects: {
      create: new ExecuteCreateProject(executionContext, idGenerator, repos.projects),
      get: new ExecuteGetProject(executionContext, idGenerator, repos.projects),
      list: new ExecuteListProjects(executionContext, idGenerator, repos.projects),
    },
    riskClassGates: {
      get: new ExecuteGetRiskClassGateMatrix(executionContext, idGenerator, repos.riskClassGates),
    },
    scaleQuota: {
      get: new ExecuteDecideProjectScaleQuota(executionContext, idGenerator, repos.scaleQuota),
    },
  };
  return { repos, useCases: useCases as ProjectsDomainModule["useCases"] };
}
