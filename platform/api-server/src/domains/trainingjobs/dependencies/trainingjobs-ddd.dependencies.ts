/**
 * Trainingjobs DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: trainingjobs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Trainingjobs Domain Module - Composition root
 *
 * DDD: Composition root for trainingjobs domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BaseWeightRepositoryAdapter,
  SpendRepositoryAdapter,
  StopRepositoryAdapter,
  TrainingJobRepositoryAdapter,
} from "@aetherlab/adapters/trainingjobs";
import { getIdGeneratorService } from "@aetherlab/adapters";
import type { AdapterDynamoDBClient } from "@aetherlab/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateTrainingJob,
  ExecuteGetTrainingJob,
  ExecuteListCloudSpend,
  ExecuteListTrainingJobs,
  ExecuteRegisterTrainingJobBaseWeight,
  ExecuteStopTrainingJob,
} from "@aetherlab/services/trainingjobs/usecases";
// Ports (interfaces)
import type {
  BaseWeightRepository,
  SpendRepository,
  StopRepository,
  TrainingJobRepository,
} from "@aetherlab/services/trainingjobs/ports";

export interface TrainingjobsDomainModule {
  repos: {
    baseWeights: BaseWeightRepository;
    spends: SpendRepository;
    stops: StopRepository;
    trainingJobs: TrainingJobRepository;
  };
  useCases: {
    baseWeights: {
      create: ExecuteRegisterTrainingJobBaseWeight;
    };
    spends: {
      list: ExecuteListCloudSpend;
    };
    stops: {
      create: ExecuteStopTrainingJob;
    };
    trainingJobs: {
      create: ExecuteCreateTrainingJob;
      get: ExecuteGetTrainingJob;
      list: ExecuteListTrainingJobs;
    };
  };
}

export function buildTrainingjobsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): TrainingjobsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      baseWeights?: BaseWeightRepository;
      spends?: SpendRepository;
      stops?: StopRepository;
      trainingJobs?: TrainingJobRepository;
    } = {};

    obj.baseWeights = new BaseWeightRepositoryAdapter(dynamoClient);
    obj.spends = new SpendRepositoryAdapter(dynamoClient);
    obj.stops = new StopRepositoryAdapter(dynamoClient);
    obj.trainingJobs = new TrainingJobRepositoryAdapter(dynamoClient);
    return {
      baseWeights: obj.baseWeights!,
      spends: obj.spends!,
      stops: obj.stops!,
      trainingJobs: obj.trainingJobs!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    baseWeights: {
      create: new ExecuteRegisterTrainingJobBaseWeight(executionContext, idGenerator, repos.baseWeights),
    },
    spends: {
      list: new ExecuteListCloudSpend(executionContext, idGenerator, repos.spends),
    },
    stops: {
      create: new ExecuteStopTrainingJob(executionContext, idGenerator, repos.stops),
    },
    trainingJobs: {
      create: new ExecuteCreateTrainingJob(executionContext, idGenerator, repos.trainingJobs),
      get: new ExecuteGetTrainingJob(executionContext, idGenerator, repos.trainingJobs),
      list: new ExecuteListTrainingJobs(executionContext, idGenerator, repos.trainingJobs),
    },
  };
  return { repos, useCases: useCases as TrainingjobsDomainModule["useCases"] };
}
