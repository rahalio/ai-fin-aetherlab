/**
 * Datasets Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: datasets
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const registerDataset_Body = z
  .object({
    projectId: z.string(),
    sourceSystem: z.string(),
    extractedAt: z.string().datetime({ offset: true }).optional(),
    purpose: z.string(),
    locationUri: z.string(),
    residency: z.string().optional(),
    qualityNotes: z.string().optional(),
  })
  .passthrough();
const createDatasetGrant_Body = z
  .object({
    grantee: z.string(),
    purpose: z.string(),
    expiresAt: z.string().datetime({ offset: true }),
  })
  .passthrough();
const attestDatasetPurpose_Body = z
  .object({ purpose: z.string(), notes: z.string().optional() })
  .passthrough();

export const schemas: any = {
  registerDataset_Body,
  createDatasetGrant_Body,
  attestDatasetPurpose_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v1/datasets',
    alias: 'listDatasets',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'projectId',
        type: 'Query',
        schema: z.string().optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  datasetId: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
                  projectId: z.string(),
                  sourceSystem: z.string(),
                  extractedAt: z.string().datetime({ offset: true }).optional(),
                  purpose: z.string(),
                  locationUri: z.string(),
                  residency: z.string().optional(),
                  qualityNotes: z.string().optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/datasets',
    alias: 'registerDataset',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: registerDataset_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            datasetId: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
            projectId: z.string(),
            sourceSystem: z.string(),
            extractedAt: z.string().datetime({ offset: true }).optional(),
            purpose: z.string(),
            locationUri: z.string(),
            residency: z.string().optional(),
            qualityNotes: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 422,
        description: `Semantically invalid request (e.g. PACK_EMPTY)`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/datasets/:datasetId',
    alias: 'getDataset',
    requestFormat: 'json',
    parameters: [
      {
        name: 'datasetId',
        type: 'Path',
        schema: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            datasetId: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
            projectId: z.string(),
            sourceSystem: z.string(),
            extractedAt: z.string().datetime({ offset: true }).optional(),
            purpose: z.string(),
            locationUri: z.string(),
            residency: z.string().optional(),
            qualityNotes: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/datasets/:datasetId/attest',
    alias: 'attestDatasetPurpose',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: attestDatasetPurpose_Body,
      },
      {
        name: 'datasetId',
        type: 'Path',
        schema: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            datasetId: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
            projectId: z.string(),
            sourceSystem: z.string(),
            extractedAt: z.string().datetime({ offset: true }).optional(),
            purpose: z.string(),
            locationUri: z.string(),
            residency: z.string().optional(),
            qualityNotes: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/datasets/:datasetId/grants',
    alias: 'listDatasetGrants',
    requestFormat: 'json',
    parameters: [
      {
        name: 'datasetId',
        type: 'Path',
        schema: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  grantId: z.string().regex(/^grn_[0-9A-HJKMNP-TV-Z]{26}$/),
                  datasetId: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
                  grantee: z.string(),
                  purpose: z.string(),
                  expiresAt: z.string().datetime({ offset: true }),
                  status: z.enum(['active', 'expired', 'revoked']),
                  attestedAt: z.string().datetime({ offset: true }).optional(),
                })
                .passthrough()
            ),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/datasets/:datasetId/grants',
    alias: 'createDatasetGrant',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: createDatasetGrant_Body,
      },
      {
        name: 'datasetId',
        type: 'Path',
        schema: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            grantId: z.string().regex(/^grn_[0-9A-HJKMNP-TV-Z]{26}$/),
            datasetId: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
            grantee: z.string(),
            purpose: z.string(),
            expiresAt: z.string().datetime({ offset: true }),
            status: z.enum(['active', 'expired', 'revoked']),
            attestedAt: z.string().datetime({ offset: true }).optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 422,
        description: `Semantically invalid request (e.g. PACK_EMPTY)`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'delete',
    path: '/v1/datasets/:datasetId/grants/:grantId',
    alias: 'revokeDatasetGrant',
    requestFormat: 'json',
    parameters: [
      {
        name: 'datasetId',
        type: 'Path',
        schema: z.string().regex(/^dst_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'grantId',
        type: 'Path',
        schema: z.string().regex(/^grn_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z.void(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
