/**
 * Monitors Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: monitors
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const createMonitor_Body = z
  .object({
    modelId: z.string(),
    endpointName: z.string().optional(),
    metric: z.enum(['drift', 'performance', 'latency', 'cost', 'business_kpi']),
    threshold: z.number(),
    alertRouting: z.string().optional(),
    retrainTrigger: z.boolean().optional(),
  })
  .passthrough();
const acknowledgeMonitor_Body = z
  .object({ notes: z.string(), openRetrain: z.boolean() })
  .partial()
  .passthrough();

export const schemas: any = {
  createMonitor_Body,
  acknowledgeMonitor_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v1/monitors',
    alias: 'listMonitors',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'modelId',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z.enum(['healthy', 'breached', 'disabled']).optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  monitorId: z.string().regex(/^mon_[0-9A-HJKMNP-TV-Z]{26}$/),
                  modelId: z.string(),
                  endpointName: z.string().optional(),
                  metric: z.enum([
                    'drift',
                    'performance',
                    'latency',
                    'cost',
                    'business_kpi',
                  ]),
                  threshold: z.number(),
                  status: z.enum(['healthy', 'breached', 'disabled']),
                  alertRouting: z.string().optional(),
                  retrainTrigger: z.boolean().optional(),
                  lastAcknowledgedAt: z
                    .string()
                    .datetime({ offset: true })
                    .optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/monitors',
    alias: 'createMonitor',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: createMonitor_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            monitorId: z.string().regex(/^mon_[0-9A-HJKMNP-TV-Z]{26}$/),
            modelId: z.string(),
            endpointName: z.string().optional(),
            metric: z.enum([
              'drift',
              'performance',
              'latency',
              'cost',
              'business_kpi',
            ]),
            threshold: z.number(),
            status: z.enum(['healthy', 'breached', 'disabled']),
            alertRouting: z.string().optional(),
            retrainTrigger: z.boolean().optional(),
            lastAcknowledgedAt: z
              .string()
              .datetime({ offset: true })
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 422,
        description: `Semantically invalid request (e.g. PACK_EMPTY)`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v1/monitors/:monitorId',
    alias: 'getMonitor',
    requestFormat: 'json',
    parameters: [
      {
        name: 'monitorId',
        type: 'Path',
        schema: z.string().regex(/^mon_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            monitorId: z.string().regex(/^mon_[0-9A-HJKMNP-TV-Z]{26}$/),
            modelId: z.string(),
            endpointName: z.string().optional(),
            metric: z.enum([
              'drift',
              'performance',
              'latency',
              'cost',
              'business_kpi',
            ]),
            threshold: z.number(),
            status: z.enum(['healthy', 'breached', 'disabled']),
            alertRouting: z.string().optional(),
            retrainTrigger: z.boolean().optional(),
            lastAcknowledgedAt: z
              .string()
              .datetime({ offset: true })
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/monitors/:monitorId/acknowledge',
    alias: 'acknowledgeMonitor',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: acknowledgeMonitor_Body,
      },
      {
        name: 'monitorId',
        type: 'Path',
        schema: z.string().regex(/^mon_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            monitorId: z.string().regex(/^mon_[0-9A-HJKMNP-TV-Z]{26}$/),
            modelId: z.string(),
            endpointName: z.string().optional(),
            metric: z.enum([
              'drift',
              'performance',
              'latency',
              'cost',
              'business_kpi',
            ]),
            threshold: z.number(),
            status: z.enum(['healthy', 'breached', 'disabled']),
            alertRouting: z.string().optional(),
            retrainTrigger: z.boolean().optional(),
            lastAcknowledgedAt: z
              .string()
              .datetime({ offset: true })
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
