/**
 * ExecuteListCloudSpend - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: trainingjobs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListCloudSpend Use Case
 *
 * DDD: Application use case for list Spend.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListCloudSpendInput, ListCloudSpendOutput } from "../dto/spend.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aetherlab/services/_shared/index.js";
import type { SpendRepository } from "../ports";

export class ExecuteListCloudSpend {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly spend: SpendRepository
  ) {}

  async execute(input: ListCloudSpendInput): Promise<ListCloudSpendOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.trnId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.spend.listCloudSpend({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListCloudSpendOutput;
  }
}
