/**
 * GetProject handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getProject } from "@aetherlab/api-server/domains/projects/handlers";
import type { ProjectsDDDDependencies } from "@aetherlab/api-server/domains/projects";
import { createProject as createProjectEntity } from "@aetherlab/factories/projects";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aetherlab/tests/shared";
import { PROJECTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetProject", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ProjectsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ProjectsDDDDependencies>(
      PROJECTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createProjectEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetProject, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { projectId: entity.id };

    await getProject(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetProject.execute).toHaveBeenCalledWith(expect.objectContaining({ projectId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { projectId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetProject, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getProject(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
