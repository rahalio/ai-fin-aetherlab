/**
 * Project Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { ProjectEntity } from "@aetherlab/core/projects/models/index.js";
import { projectsSchemas } from "@aetherlab/core/projects";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "prj";

export function createProject(overrides: Partial<ProjectEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    name: overrides.name ?? faker.person.fullName(),
    owner: overrides.owner ?? faker.lorem.word(),
    projectId: overrides.projectId ?? `prj_${ulid().toLowerCase()}`,
    riskClass: overrides.riskClass ?? "low",
    status: overrides.status ?? "draft",
    ...overrides,
  };

  return projectsSchemas.Project.parse(raw);
}

export function projectBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createProject(overrides));
}
