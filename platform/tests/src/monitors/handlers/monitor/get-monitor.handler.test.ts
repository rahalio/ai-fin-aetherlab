/**
 * GetMonitor handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: monitors
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getMonitor } from "@aetherlab/api-server/domains/monitors/handlers";
import type { MonitorsDDDDependencies } from "@aetherlab/api-server/domains/monitors";
import { createMonitor as createMonitorEntity } from "@aetherlab/factories/monitors";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aetherlab/tests/shared";
import { MONITORS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetMonitor", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: MonitorsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<MonitorsDDDDependencies>(
      MONITORS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createMonitorEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetMonitor, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { monitorId: entity.id };

    await getMonitor(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetMonitor.execute).toHaveBeenCalledWith(expect.objectContaining({ monitorId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { monitorId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetMonitor, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getMonitor(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
