/**
 * ExecuteDeployment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deployments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { executeDeployment } from "@aetherlab/api-server/domains/deployments/handlers";
import type { DeploymentsDDDDependencies } from "@aetherlab/api-server/domains/deployments";
import { createCreateDeployRequest } from "@aetherlab/factories/deployments";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aetherlab/tests/shared";
import { DEPLOYMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("ExecuteDeployment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DeploymentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DeploymentsDDDDependencies>(
      DEPLOYMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateDeployRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeExecuteDeployment, "execute").mockResolvedValue(useCaseResult);

    await executeDeployment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeExecuteDeployment.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
