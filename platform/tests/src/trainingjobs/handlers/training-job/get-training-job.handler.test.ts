/**
 * GetTrainingJob handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: trainingjobs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getTrainingJob } from "@aetherlab/api-server/domains/trainingjobs/handlers";
import type { TrainingjobsDDDDependencies } from "@aetherlab/api-server/domains/trainingjobs";
import { createTrainingJob as createTrainingJobEntity } from "@aetherlab/factories/trainingjobs";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aetherlab/tests/shared";
import { TRAININGJOBS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetTrainingJob", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TrainingjobsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TrainingjobsDDDDependencies>(
      TRAININGJOBS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createTrainingJobEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetTrainingJob, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { trainingJobId: entity.id };

    await getTrainingJob(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetTrainingJob.execute).toHaveBeenCalledWith(expect.objectContaining({ trainingJobId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { trainingJobId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetTrainingJob, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getTrainingJob(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
