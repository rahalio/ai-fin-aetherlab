/**
 * GetDataset handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: datasets
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDataset } from "@aetherlab/api-server/domains/datasets/handlers";
import type { DatasetsDDDDependencies } from "@aetherlab/api-server/domains/datasets";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@aetherlab/tests/shared";
import { DATASETS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDataset", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DatasetsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DatasetsDDDDependencies>(
      DATASETS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDataset, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { datasetId: entity.id };

    await getDataset(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDataset.execute).toHaveBeenCalledWith(expect.objectContaining({ datasetId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { datasetId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDataset, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDataset(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
